---
:schema: 1
:title: Miniconda3
:license: BSD License
:summary: Free minimal installer for conda
:url: https://docs.anaconda.com/free/miniconda/
:description: |
  Miniconda is a free minimal installer for conda. It is a small
  bootstrap version of Anaconda that includes only conda, Python, the
  packages they both depend on, and a small number of other useful
  packages (like pip, zlib, and a few others). If you need more
  packages, use the conda install command to install from thousands of
  packages available by default in Anaconda’s public repo, or from
  other channels, like conda-forge or bioconda.
:group: Languages
:changelog: |
  * Wed Jun 5 2024 - Mark J. Titorenko <mark.titorneko@alces-flight.com>
    - First created
:src: Miniconda3-py312_24.4.0-0-Linux-x86_64.sh.sic
:src_dir: .
:type: apps
:version: '24.4.0'
:compilers:
  bin:
:compile: |
  # no-op; this is a distribution of a binary package
:install: |
  ln -s <%= source('Miniconda3-py312_24.4.0-0-Linux-x86_64.sh.sic') %> install.sh
  sh install.sh -f -b -p <%= dest_dir %> <%= redirect(:install) %>
:module: |
  setenv ${appcaps}DIR ${appdir}
  setenv ${appcaps}BIN ${appdir}/bin

  prepend-path PATH ${appdir}/bin

  conflict apps/anaconda
  conflict apps/anaconda3
  conflict apps/python3
  conflict apps/python
