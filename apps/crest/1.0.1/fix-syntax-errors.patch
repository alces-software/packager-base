--- CREST.pl.orig	2015-02-16 12:13:19.083449898 +0000
+++ CREST.pl	2015-02-16 12:13:44.790537759 +0000
@@ -532,11 +532,12 @@
 	my ($a, $p) = @_;
 	my ($s, $e) = (0, scalar(@{$a})-1);
 #	my $m = int( ($s + $e)/2);
+	my $m = 0;
 	while(1) {
 		return $s if($a->[$s][0] >= $p);	
 		return $e if($a->[$e][0] <= $p);
 #		return $m if($a->[$m][0] == $p || ($a->[$m-1][0] < $p && $a->[$m+1][0] > $p));
-		my ($rs, $re) = ($m>0 ? $a->[$m-1][0] : $a->[0][0], $m<scalar(@{$a})-1 ? $a->[$m+1][0] : $a->[-1][0];
+		my ($rs, $re) = ($m>0 ? $a->[$m-1][0] : $a->[0][0], $m<scalar(@{$a})-1 ? $a->[$m+1][0] : $a->[-1][0]);
 		return $m if($a->[$m][0] == $p || ($rs < $p && $re >  $p));
 
 		if($a->[$m][0] > $p) {
